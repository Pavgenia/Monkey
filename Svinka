
#include <iostream>
using namespace std; //для расчёта площади прямоугольника

int main() 
{ 
     float length, width;
    cin >> length >> width;
    cout << (length + width) * 2;
    return 0; 
}
